// predictable expenses into an associative array (auto_id, name, expense value, date value)

// predictable income into an associative array (auto_id, name, income value, date value)

// entries associative array (auto_id, name, balance, date value)

//should display assumed current balance with entries data & other data in account. This would have to be AJAXed so new data can be entered, and effect the display.
